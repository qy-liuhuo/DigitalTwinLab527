dubbo:
  application:
    name: dtlab-consumer
    qos-enable: false
    serialize-check-status: WARN

  protocol:
    name: dubbo
    port: -1
  registry:
    address: zookeeper://${zookeeper.address:127.0.0.1}:2181

server:
  port: 8080
